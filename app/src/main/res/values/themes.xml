<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.hcmurs" parent="android:Theme.Material.Light.NoActionBar" />

    <style name="Theme.MVVM.Splash" parent="Theme.SplashScreen">
        <!-- Đặt màu nền splash -->
        <item name="windowSplashScreenBackground">@color/white</item>
        <!-- Đặt icon hoặc logo -->
        <item name="windowSplashScreenAnimatedIcon">@drawable/hurc</item>
        <item name="windowSplashScreenIconBackgroundColor">@color/white</item>
        <item name="postSplashScreenTheme">@style/Theme.hcmurs</item>
    </style>

</resources>